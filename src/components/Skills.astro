---
---

<section id="skills" class="px-6 py-40">
  <div class="mx-auto max-w-6xl">
    <h2 data-reveal class="gsap-reveal mb-20 text-center font-heading text-[2.25rem] font-medium tracking-[-0.02em] text-text-primary">Tech Stack</h2>

    <!-- Globe -->
    <div class="flex items-center justify-center">
      <canvas
        id="globe-canvas"
        class="aspect-square w-full max-w-[720px] cursor-grab active:cursor-grabbing"
        style="touch-action: pan-y;"
      ></canvas>
    </div>
  </div>
</section>

<script>
  import { initGlobe } from "../scripts/globe";

  const canvas = document.getElementById("globe-canvas") as HTMLCanvasElement | null;
  if (canvas) {
    initGlobe(canvas);
  }
</script>
